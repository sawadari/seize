# 🎯 Seize - 統一エージェント方程式 + 要求工学

**人間中心AI時代の組織憲章に基づく、完全な統一エージェントフレームワーク**

[![Rust](https://img.shields.io/badge/rust-%23000000.svg?style=for-the-badge&logo=rust&logoColor=white)](https://www.rust-lang.org/)
[![License: MIT OR Apache-2.0](https://img.shields.io/badge/License-MIT%20OR%20Apache--2.0-blue.svg)](LICENSE)

---

## 🌟 概要

**Seize**は、統一エージェント方程式に基づいた次世代AIエージェントフレームワークです。

**NEW**: システムズエンジニアリングおよび要求工学（ISO/IEC/IEEE 29148準拠）を完全実装！

### 統一エージェント方程式

```
𝔸(Input, World₀) = lim_{n→∞} [∫₀ⁿ (Θ ◦ 𝒞 ◦ ℐ)(t) dt] = World_∞
```

**構成要素:**
- **ℐ**: Intent Resolution (意図解決) - 曖昧な入力から明確な目標へ
- **𝒞**: Command Stack (コマンドスタック) - 目標を実行可能なタスクに分解
- **Θ**: World Transformation (世界変換) - 6つの変換フェーズによる世界状態の更新

### 応用: 要求工学

```
要求開発 = 𝔸(ステークホルダーニーズ, World₀) → 要求仕様書
```

統一エージェント方程式を要求工学に適用し、ISO/IEC/IEEE標準に準拠した要求開発プロセスを実装。

---

## 🚀 クイックスタート

### 前提条件

- Rust 1.70以上
- Cargo

### インストール

```bash
# リポジトリのクローン
git clone https://github.com/yourusername/seize.git
cd seize

# ビルド
cargo build --release

# インストール
cargo install --path crates/seize-cli
```

### 基本的な使用方法

#### 統一エージェントの実行

```bash
# エージェントを実行
seize run --input "ファイルを読み込んで分析してください"

# 統一エージェント方程式を表示
seize formula

# 世界の初期状態を表示
seize init --format pretty
```

#### 要求工学の使用

```bash
# 要求プロジェクトの初期化
seize requirements init --project "MyProject"

# ステークホルダーインタビューから要求を抽出
seize requirements elicit \
  --input "interview.md" \
  --stakeholder "Product Owner" \
  --output "requirements.json"

# 要求の品質を分析
seize requirements analyze \
  --spec "requirements.json" \
  --report "analysis.md"

# トレーサビリティマトリクスを生成
seize requirements trace \
  --spec "requirements.json" \
  --format "markdown"
```

---

## 📦 プロジェクト構成

```
seize/
├── Cargo.toml                 # Workspace設定
├── crates/
│   ├── seize-core/           # コアライブラリ
│   │   ├── src/
│   │   │   ├── lib.rs        # モジュールエクスポート
│   │   │   ├── world.rs      # 世界モデル
│   │   │   ├── intent.rs     # ℐ: 意図解決
│   │   │   ├── command.rs    # 𝒞: コマンドスタック
│   │   │   ├── transformation.rs # Θ: 世界変換
│   │   │   ├── agent.rs      # 統一エージェント
│   │   │   └── requirements.rs # 要求工学モジュール ⭐NEW
│   │   └── Cargo.toml
│   └── seize-cli/            # CLIインターフェース
│       ├── src/
│       │   ├── main.rs
│       │   └── requirements_handler.rs # 要求コマンドハンドラー ⭐NEW
│       └── Cargo.toml
├── .claude/                   # Claude Code統合
│   ├── agents/
│   │   ├── seize-agent.md    # 統一エージェント専門家
│   │   └── systems-engineer.md # システムズエンジニア専門家 ⭐NEW
│   └── commands/
│       ├── seize-build.md
│       └── seize-doc.md
└── docs/
    ├── YOUR_DESIGN.md        # 要求工学実践ガイド ⭐NEW
    ├── QUICKSTART.md
    └── ...
```

---

## 🎓 コアコンセプト

### 1. 統一エージェント方程式

#### 世界モデル (World)

世界は以下の要素で構成されます：

- **ファイルシステム**: プロジェクトのファイル状態
- **実行コンテキスト**: 作業ディレクトリ、環境変数、履歴
- **知識ベース**: 組織の原則、学習内容、意思決定記録
- **メタデータ**: その他の状態情報

#### 意図解決 (Intent Resolution)

**Step-Back Question**により、表面的な要求から本質的な目的を引き出します。

#### コマンドスタック (Command Stack)

目標を実行可能なタスクに3段階で分解：

1. **C₁**: 構造化 - タスクツリーへの分解
2. **C₂**: プロンプト化 - 実行可能なプロンプトに変換
3. **C₃**: 連鎖実行 - 依存関係を考慮した実行順序

#### 世界変換 (World Transformation)

6つの変換フェーズ：

- **θ₁: Understand** - 理解
- **θ₂: Generate** - 生成
- **θ₃: Allocate** - 配分
- **θ₄: Execute** - 実行
- **θ₅: Integrate** - 統合
- **θ₆: Learn** - 学習

### 2. 要求工学 ⭐NEW

**ISO/IEC/IEEE 29148準拠**の完全な要求工学実装

#### 要求の種類

- **機能要求** (Functional Requirements)
- **非機能要求** (Non-Functional Requirements)
  - パフォーマンス、セキュリティ、信頼性、可用性、保守性、スケーラビリティ、ユーザビリティ
- **システム要求** (System Requirements)
- **ビジネス要求** (Business Requirements)
- **ユーザー要求** (User Requirements)
- **制約** (Constraints)
- **インターフェース要求** (Interface Requirements)

#### 要求の特性 (5C原則)

| 特性 | 説明 |
|------|------|
| **Clear** (明確性) | 曖昧さがなく一意に解釈できる |
| **Verifiable** (検証可能性) | テスト可能な受入基準がある |
| **Complete** (完全性) | 必要な情報がすべて含まれる |
| **Consistent** (一貫性) | 他の要求と矛盾しない |
| **Feasible** (実現可能性) | 技術的・経済的に実現可能 |

#### MoSCoW優先順位付け

- **Must have** (必須) - リリースに不可欠
- **Should have** (重要) - 可能な限り含める
- **Could have** (可能なら) - リソースがあれば実装
- **Won't have** (今回は対象外) - 将来的に検討

#### Given-When-Then受入基準

```
Given: ユーザーがログインフォームにアクセスした時
When: 正しいユーザー名とパスワードを入力する
Then: システムにログインしてホーム画面に遷移する
```

#### トレーサビリティ

```
ビジネス要求 → ユーザー要求 → システム要求 → 機能要求 → 設計 → 実装 → テスト
```

---

## 🏛️ 人間中心AI時代の組織憲章

このプロジェクトは「人間中心AI時代の組織憲章」に準拠しています。

### 核心原則

1. **人間は判断する** - AIは情報を示し、最終判断は人間が行う
2. **意図を持って問いを立てる** - 正しい問いを生み出す力を尊ぶ
3. **公正と透明性** - すべての意思決定を記録し、説明可能性を維持
4. **目的の明確化** - 利用前に「何を・なぜ・どのように」を定義

詳細: `.claude/YOUR_DOMAIN_LOGIC.md`

---

## 🛠️ 開発

### ビルド

```bash
cargo build --workspace
```

### テスト

```bash
cargo test --workspace
```

### ドキュメント生成

```bash
cargo doc --workspace --no-deps --open
```

### コードフォーマット

```bash
cargo fmt --all
```

### Clippy

```bash
cargo clippy --workspace -- -D warnings
```

---

## 🔌 Claude Code統合

### カスタムエージェントの使用

```bash
# 統一エージェント専門家
/task --agent seize-agent "新しい変換フェーズを追加"

# システムズエンジニア専門家 ⭐NEW
/task --agent systems-engineer "新機能の要求を抽出・分析"
```

### カスタムコマンド

```bash
# ビルドとテスト
/seize-build

# ドキュメント生成
/seize-doc
```

---

## 📚 ドキュメント

- **[クイックスタート](docs/QUICKSTART.md)** - すぐに始める
- **[要求工学実践ガイド](docs/YOUR_DESIGN.md)** ⭐NEW - システムズエンジニアリング
- **[デプロイガイド](docs/DEPLOYMENT_GUIDE.md)** - 本番環境への展開
- **[統一エージェント方程式](CLAUDE.md)** - 理論的基盤
- **[組織憲章](.claude/YOUR_DOMAIN_LOGIC.md)** - 倫理的指針

---

## 🌟 使用例

### 例1: 基本的なエージェント実行

```rust
use seize_core::{UnifiedAgent, World};

fn main() -> anyhow::Result<()> {
    let agent = UnifiedAgent::default();
    let world = World::new();

    let result = agent.run("ファイルを分析してください", world)?;

    println!("反復回数: {}", result.iterations);
    println!("収束: {}", result.converged);

    Ok(())
}
```

### 例2: 要求工学ワークフロー ⭐NEW

```rust
use seize_core::{RequirementsEngineer, Requirement, RequirementType};

fn main() -> anyhow::Result<()> {
    // 要求エンジニアを作成
    let mut engineer = RequirementsEngineer::new("MyProject".to_string(), true);

    // ステークホルダーインタビューから要求を抽出
    let interview = "システムはユーザー認証機能を提供する必要がある。
                     ログイン処理は1秒以内に完了するべき。";
    let requirements = engineer.elicit_requirements(interview, "Product Owner")?;

    // 各要求を分析
    for req in &requirements {
        let analysis = engineer.analyze_requirement(req)?;

        if analysis.is_valid {
            println!("✅ {}: 有効", req.id);
            engineer.add_requirement(req.clone())?;
        } else {
            println!("⚠️ {}: 問題あり", req.id);
            for issue in &analysis.issues {
                println!("  - {}", issue);
            }
        }
    }

    // トレーサビリティマトリクスを生成
    let matrix = engineer.analyze_traceability();
    println!("トレース可能な要求: {}", matrix.requirements.len());

    Ok(())
}
```

### 例3: 意思決定の記録

```rust
use seize_core::world::{World, Decision};

let mut world = World::new();

let decision = Decision {
    timestamp: "2025-01-15T10:00:00Z".to_string(),
    purpose: "新機能の実装".to_string(),
    input: "ユーザーからのフィードバック".to_string(),
    options: vec!["実装A".to_string(), "実装B".to_string()],
    rationale: "パフォーマンスと保守性のバランス".to_string(),
    approver: "Team Lead".to_string(),
    impact_scope: "全ユーザー".to_string(),
    alternatives: vec!["段階的ロールアウト".to_string()],
    revocation_conditions: vec!["重大なバグ発生".to_string()],
};

world.record_decision(decision);
```

---

## 🎯 ロードマップ

### 完了

- [x] コアフレームワークの実装
- [x] CLI基本機能
- [x] Claude Code統合
- [x] **要求工学モジュール** ⭐NEW
- [x] **システムズエンジニアエージェント** ⭐NEW
- [x] **ISO/IEC/IEEE 29148準拠** ⭐NEW

### 今後の予定

- [ ] Web UIの追加
- [ ] プラグインシステム
- [ ] 分散実行サポート
- [ ] 高度な収束アルゴリズム
- [ ] LLM統合（AI支援要求抽出）
- [ ] リスク分析機能
- [ ] 要求変更管理
- [ ] 自動テストケース生成

---

## 🤝 コントリビューション

コントリビューションを歓迎します！

1. このリポジトリをフォーク
2. フィーチャーブランチを作成 (`git checkout -b feature/amazing-feature`)
3. 変更をコミット (`git commit -m 'Add amazing feature'`)
4. ブランチにプッシュ (`git push origin feature/amazing-feature`)
5. プルリクエストを作成

---

## 📄 ライセンス

このプロジェクトはMITまたはApache-2.0のデュアルライセンスです。

---

## 📞 サポート

- Issues: [GitHub Issues](https://github.com/yourusername/seize/issues)
- Discussions: [GitHub Discussions](https://github.com/yourusername/seize/discussions)

---

## 🏆 主な特徴

### 統一エージェント方程式

- ✅ 完全な数学的定式化
- ✅ 瞬く景色（Flickering Scenery）世界モデル
- ✅ 6つの変換フェーズ（θ₁〜θ₆）
- ✅ 収束アルゴリズム

### 要求工学 ⭐NEW

- ✅ ISO/IEC/IEEE 29148準拠
- ✅ 5C原則（Clear, Verifiable, Complete, Consistent, Feasible）
- ✅ MoSCoW優先順位付け
- ✅ Given-When-Then受入基準
- ✅ 完全なトレーサビリティ
- ✅ 組織憲章統合

### 人間中心AI

- ✅ 人間の最終判断
- ✅ 意思決定の記録
- ✅ 説明可能性の維持
- ✅ 最小限データ原則

---

**Made with ❤️ using the Unified Agent Formula**

**要求工学とシステムズエンジニアリングの力で、より良いシステムを構築しましょう！**
